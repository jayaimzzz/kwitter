import React from "react";

const styles = {
  Nav: {
    margin: "auto",
    background: "#e8703b",
    color: "white",
    textAlign: "center",
    width: "40vw",
    height: "7vh"
  }
};

const Nav = props => <div style={styles.Nav}>The Nav</div>;

export default Nav;
