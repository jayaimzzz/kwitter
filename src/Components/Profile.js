import React from "react";

const styles = {
  Profile: {
    margin: "auto",
    background: "#ffee87",
    textAlign: "center",
    // width: "40vw",
    height: "30vh"
  }
};

const Profile = props => <div style={styles.Profile}>The Profile</div>;

export default Profile;
